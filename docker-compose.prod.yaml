services:
  web:
    command: ["gunicorn", "comic.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "3"]
    environment:
      - DJANGO_DEBUG=False
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
